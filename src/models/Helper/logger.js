/////////////////////////////////////////////////////////////////////////////////////////////
////////STILL TESTING
//////////////////////////////////////////////////////////////////////////////////////////////

const db = require('../../db')

module.export.logger = function(promise, label){
  return promise.then((data) => {




    return data
  })
}
